@import '../../utilities/vars'
@import '../../utilities/mixins'

img {}

.img-responsive
  width 100%

.vector-plane
  position absolute
  bottom -127px
  left 15px
  right 15px
  z-index 1
  +mobile()
    display none
  .plane
    position absolute
    right: 13px;
    top: -113px;
  .vector
    +portrait()
      max-width 100%
    +tablet()
      max-width 100%